const PREPREND = 'bsmnt'
const LIBRARY_NAME = 'tempo'
export const SHADOW_ROOT_ID = `${PREPREND}-shadow-root`
export const VISUALIZER_PORTAL_ID = `${PREPREND}-${LIBRARY_NAME}-portal`
export const MOUNT_EVENT_KEY = `${PREPREND}-${LIBRARY_NAME}-mounted`